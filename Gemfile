source "https://rubygems.org"

# GitHub Pages builds the site automatically — this Gemfile is only for local preview.
gem "github-pages", group: :jekyll_plugins
gem "webrick"   # required for `jekyll serve` on Ruby 3+
